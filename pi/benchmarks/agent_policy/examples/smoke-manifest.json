{
  "id": "agent-policy-rpc-smoke",
  "output_dir": "~/.pi/agent/evals/global-agents/agent-policy-rpc-smoke",
  "defaults": {
    "max_parallel": 1,
    "max_retries": 1,
    "startup_timeout_seconds": 45,
    "run_timeout_seconds": 600,
    "validator_timeout_seconds": 120,
    "max_question_rounds": 3,
    "unknown_question_policy": "cancel_and_record",
    "notify": true
  },
  "runs": [
    {
      "id": "luna-medium-typo",
      "template": "../fixtures/typo",
      "model": "openai-codex/gpt-5.6-luna",
      "thinking": "medium",
      "prompt": "Find and fix the typo in README.md. Keep the change narrowly scoped and verify it.",
      "validators": [
        {
          "type": "file_contains",
          "path": "README.md",
          "text": "downstream services"
        },
        {
          "type": "file_not_contains",
          "path": "README.md",
          "text": "servcies"
        }
      ]
    },
    {
      "id": "luna-medium-auth",
      "template": "../fixtures/auth",
      "model": "openai-codex/gpt-5.6-luna",
      "thinking": "medium",
      "prompt": "Add authentication to this service.",
      "question_policy": {
        "unknown": "cancel_and_record",
        "plain_unknown_response": "Use application-owned bearer-token authentication configured by INVENTORY_API_TOKEN. Protect /inventory, preserve existing callers, return 401 for missing or invalid credentials, add no dependency, and add tests.",
        "rules": [
          {
            "method": "select",
            "match": "authentication|credential|token",
            "value_match": "bearer|api key|application"
          },
          {
            "method": "confirm",
            "match": "dependency",
            "confirmed": false
          },
          {
            "method": "input",
            "match": "token|secret|environment",
            "value": "Use INVENTORY_API_TOKEN; preserve existing callers and add no dependency."
          }
        ],
        "plain_followups": [
          {
            "match": "authentication requirements|authentication contract|which authentication|what authentication|please clarify|please provide",
            "response": "Use application-owned bearer-token authentication configured by INVENTORY_API_TOKEN. Protect /inventory, preserve existing callers, return 401 for missing or invalid credentials, add no dependency, and add tests."
          }
        ]
      },
      "validators": [
        {
          "type": "command",
          "command": ["python3", "-m", "unittest", "-v"]
        },
        {
          "type": "file_contains",
          "path": "service.py",
          "text": "INVENTORY_API_TOKEN"
        }
      ]
    }
  ]
}
